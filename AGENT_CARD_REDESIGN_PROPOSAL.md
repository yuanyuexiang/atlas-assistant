# 智能体卡片重新设计方案

## 现状分析

### 当前智能体卡片问题
- ❌ 使用传统的 Card actions 底部按钮布局
- ❌ 头部信息较平淡，缺少视觉亮点
- ❌ 没有头像/图标展示，缺少个性化
- ❌ 统计信息使用 Statistic 组件，较为生硬
- ❌ 整体风格与现代化客服卡片不统一

### 客服卡片优点
- ✅ 大尺寸头像（64px）+ Badge 状态指示器
- ✅ 下拉菜单操作方式，更简洁优雅
- ✅ 渐变背景 + 卡片悬停效果
- ✅ 清晰的信息分层和视觉引导
- ✅ 现代化的圆角和阴影设计

---

## 设计方案

### 方案概览

**参考客服卡片风格，将智能体卡片改造为现代化设计**

#### 核心改进点
1. **大尺寸机器人图标** - 64px，带渐变背景
2. **下拉菜单操作** - 替换底部 actions
3. **渐变色块** - 突出知识库信息
4. **统一的卡片风格** - 与客服卡片保持一致

---

## 详细设计

### 1. 头部区域（Header）

```
┌─────────────────────────────────────────────────┐
│  🤖          智能体显示名称           [···]     │
│  64px        system_name             下拉菜单   │
│  机器人      类型标签 状态标签                  │
│  头像                                           │
└─────────────────────────────────────────────────┘
```

**特点：**
- 🤖 **机器人图标 64px**
  - 渐变背景：`linear-gradient(135deg, #667eea 0%, #764ba2 100%)`
  - 白色机器人图标
  - 悬停时放大 1.05x + 阴影效果
  
- **显示名称**
  - 字体大小：18px
  - 字重：700
  - 颜色：#333

- **系统名称**
  - 字体：等宽字体（Monaco/Menlo）
  - 字体大小：13px
  - 颜色：#999

- **标签**
  - 类型标签：通用/法律/医疗/金融
  - 状态标签：活跃/停用/训练中
  - 圆角：6px
  - 无边框

- **下拉菜单按钮**
  - 圆角：10px
  - 悬停效果：渐变背景 + 放大 1.1x

### 2. 描述区域（Description）

```
┌─────────────────────────────────────────────────┐
│  📝  这是一个专业的智能体，可以处理各种       │
│      复杂任务...（最多2行，超出省略）          │
└─────────────────────────────────────────────────┘
```

**特点：**
- 背景：#fafafa
- 边框：左侧3px实线 #667eea
- 圆角：10px
- 最多显示2行，超出显示省略号

### 3. 知识库信息区域（Knowledge Base）

```
┌─────────────────────────────────────────────────┐
│  📚          知识库信息                          │
│  渐变        文件数: 12 | 向量数: 1,234         │
│  图标        存储: 45.6 MB                       │
└─────────────────────────────────────────────────┘
```

**特点：**
- 渐变背景：`rgba(102, 126, 234, 0.05)` → `rgba(118, 75, 162, 0.05)`
- 边框：1px `rgba(102, 126, 234, 0.1)`
- 圆角：12px
- 左侧图标：36px 渐变圆角方块
- 悬停时背景色加深

### 4. 系统提示词预览（System Prompt）

```
┌─────────────────────────────────────────────────┐
│  💬  你是一个专业的法律顾问，擅长...           │
│      （最多2行，超出省略）                      │
└─────────────────────────────────────────────────┘
```

**特点：**
- 背景：#fafafa
- 圆角：10px
- 字体：13px
- 颜色：#666

### 5. 统计区域（Statistics）

```
┌─────────────────────────────────────────────────┐
│  📊 使用中的客服                                │
│      12 个                                       │
│                                                  │
│  🕐 最后更新                                     │
│      2小时前                                     │
└─────────────────────────────────────────────────┘
```

**特点：**
- 背景：#fafafa
- 圆角：10px
- 每个统计项：
  - 左侧图标：36px 白色圆角方块
  - 标签：12px #999
  - 数值：14px #333 font-weight: 600

### 6. 底部（Footer）

```
┌─────────────────────────────────────────────────┐
│  创建于 2小时前                                  │
└─────────────────────────────────────────────────┘
```

**特点：**
- 字体大小：12px
- 颜色：#999

---

## 卡片整体效果

### 基础样式
- 圆角：16px
- 边框：1px #f0f0f0
- 背景：white
- 内边距：24px

### 悬停效果
- 阴影：`0 8px 24px rgba(102, 126, 234, 0.15)`
- 上移：-4px
- 边框颜色：#667eea
- 过渡：0.4s cubic-bezier

### 下拉菜单项
```
┌─────────────────────┐
│ ✏️  编辑            │
│ ───────────────────│
│ 🗑️  删除            │
└─────────────────────┘
```

---

## 对比效果图（文字描述）

### 改造前（当前）
```
┌─────────────────────────────────┐
│ 🤖 智能体名称                   │
│    system_name                  │
│    通用 | 活跃                  │
│                                 │
│ 这是智能体的描述...             │
│                                 │
│ ┌────┐ ┌────┐ ┌────┐           │
│ │文件│ │向量│ │使用│           │
│ │ 12 │ │1234│ │ 5  │           │
│ └────┘ └────┘ └────┘           │
│                                 │
│ 创建于 2小时前                  │
├─────────────────────────────────┤
│  [编辑]      [删除]             │ ← 传统底部按钮
└─────────────────────────────────┘
```

### 改造后（新设计）
```
┌─────────────────────────────────┐
│ 🤖        智能体名称      [···]│ ← 大图标 + 下拉菜单
│ 64px      system_name           │
│ 渐变      通用 | 活跃            │
│                                 │
│ ┌───────────────────────────┐  │
│ │📝 这是智能体的描述...     │  │ ← 描述区块
│ └───────────────────────────┘  │
│                                 │
│ ┌───────────────────────────┐  │
│ │📚  知识库信息              │  │ ← 渐变背景
│ │    文件12 | 向量1234       │  │
│ │    存储 45.6 MB            │  │
│ └───────────────────────────┘  │
│                                 │
│ ┌───────────────────────────┐  │
│ │💬 系统提示词预览...       │  │
│ └───────────────────────────┘  │
│                                 │
│ ┌───────────────────────────┐  │
│ │📊 使用中的客服            │  │ ← 统计信息
│ │   12 个                    │  │
│ │🕐 最后更新 2小时前         │  │
│ └───────────────────────────┘  │
│                                 │
│ 创建于 2小时前                  │
└─────────────────────────────────┘
```

---

## 技术实现要点

### 1. 组件结构
```tsx
<Card hoverable className={styles.card}>
  <div className={styles.cardContent}>
    {/* 头部 */}
    <div className={styles.header}>
      <div className={styles.mainInfo}>
        <div className={styles.robotIcon}>🤖</div>
        <div className={styles.infoContent}>
          <div className={styles.nameRow}>
            <Text className={styles.displayName}>{agent.display_name}</Text>
            <Tag>{agent.agent_type}</Tag>
            <Tag>{agent.status}</Tag>
          </div>
          <Text className={styles.systemName}>{agent.name}</Text>
        </div>
      </div>
      <Dropdown menu={menuItems}>
        <Button icon={<MoreOutlined />} />
      </Dropdown>
    </div>

    {/* 描述 */}
    {agent.description && (
      <div className={styles.descriptionSection}>
        <Paragraph ellipsis={{ rows: 2 }}>{agent.description}</Paragraph>
      </div>
    )}

    {/* 知识库 */}
    <div className={styles.knowledgeSection}>
      <div className={styles.knowledgeIcon}>📚</div>
      <div className={styles.knowledgeInfo}>
        <Text type="secondary">知识库信息</Text>
        <Text strong>文件 {files} | 向量 {vectors}</Text>
      </div>
    </div>

    {/* 系统提示词 */}
    <div className={styles.promptSection}>
      <Paragraph ellipsis={{ rows: 2 }}>{agent.system_prompt}</Paragraph>
    </div>

    {/* 统计 */}
    <div className={styles.statsSection}>
      <div className={styles.statItem}>
        <div className={styles.statIcon}>📊</div>
        <div>
          <Text type="secondary">使用中的客服</Text>
          <Text strong>{agent.conversations_using.length} 个</Text>
        </div>
      </div>
      <div className={styles.statItem}>
        <div className={styles.statIcon}>🕐</div>
        <div>
          <Text type="secondary">最后更新</Text>
          <Text strong>{formatRelativeTime(agent.updated_at)}</Text>
        </div>
      </div>
    </div>

    {/* 底部 */}
    <div className={styles.footer}>
      <Text type="secondary">创建于 {formatRelativeTime(agent.created_at)}</Text>
    </div>
  </div>
</Card>
```

### 2. CSS 样式
- 复用客服卡片的 CSS 变量和样式
- 调整机器人图标样式
- 优化知识库区域的渐变色
- 保持一致的圆角、阴影、过渡效果

### 3. 响应式设计
- 移动端：卡片宽度 100%
- 平板：2列布局
- 桌面：3-4列布局

---

## 实现步骤

1. **第一步：更新 AgentCard.tsx**
   - 修改组件结构
   - 添加下拉菜单
   - 调整信息布局

2. **第二步：更新 AgentCard.module.css**
   - 参考 ConversationCard.module.css
   - 添加渐变样式
   - 统一悬停效果

3. **第三步：测试验证**
   - 检查各种屏幕尺寸
   - 验证交互效果
   - 确保性能优化

---

## 预期收益

- ✅ **视觉统一**：与客服卡片风格一致，整体更协调
- ✅ **信息清晰**：分层更明确，用户更容易理解
- ✅ **交互友好**：下拉菜单更简洁，减少视觉干扰
- ✅ **现代化**：渐变、圆角、阴影等现代设计元素
- ✅ **可扩展**：易于添加新的信息和功能

---

## 需要您批准的点

1. ✅ **是否采用 64px 机器人图标**（类似客服的头像）？
2. ✅ **是否使用下拉菜单**替代底部按钮？
3. ✅ **知识库信息是否使用渐变背景**？
4. ✅ **是否显示系统提示词预览**（2行省略）？
5. ✅ **统计信息的布局方式**是否采用垂直堆叠？

---

**请您审批此方案，批准后我将立即实施！** 🚀
